# More Functions and Nested Loops

This project is part of the Holberton School low-level programming curriculum.  
It focuses on practicing **functions**, **nested loops**, and understanding how to structure C programs.

---

## 📂 Project Files

- `0-isupper.c` → Function that checks for uppercase characters.
- `main.h` → Header file containing function prototypes.
- (Add other files as you progress...)

---

## 📖 Function Descriptions

### `_isupper(int c)`
- **Prototype:** `int _isupper(int c);`
- **Description:** Returns `1` if `c` is an uppercase character, otherwise returns `0`.

---

## 🚀 Compilation

All files are compiled using:
```bash
gcc -Wall -Werror -Wextra -pedantic -std=gnu89 *.c -o <output_name>

